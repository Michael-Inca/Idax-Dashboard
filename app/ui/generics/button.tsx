import React from "react";

export default function PrimaryButton({children}: {children: React.ReactNode}){
    <button className="mx-2.5 w-48 h-8 rounded-full bg-blue-500 text-base text-white">
        {children}
    </button>
}

export function CardButton({children}: {children: React.ReactNode}){
    
}